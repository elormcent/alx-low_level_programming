#include "main.h"

/**
 * _isdigit - Check if a character is a digit
 * @x: The number to be checked
 * Return: 1 digit or 0 for any
 */

int _isdigit(int x)
{

	if (x > -1 && x < 10)
	{
	return (1);
	}
	return (0);
}
