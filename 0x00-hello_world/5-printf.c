#include <stdio.h>

/**
 * main - entry point of the program or instructions
 * Return: always 0 to indicate success
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
