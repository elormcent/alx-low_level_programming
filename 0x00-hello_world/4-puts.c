#include <stdio.h>

/**
 * int  main() is the  entry point
 * return is  Always 0 due to intructions and the void returns
 * program is to -> print Programming is like building a multilingual puzzle
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");	
	return (0);
}
